const AttendanceSheet = ({ classId, armId }: { classId: string, armId: string }) => {
  console.log('classId', classId);
  console.log('armId', armId);
  return (
    <>Attendance</>
  );
};

export default AttendanceSheet;